services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - "${HOME}/.todoapp:/root/.todoapp"

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
