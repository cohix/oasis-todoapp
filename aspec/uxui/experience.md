# Experience

The application should automatically load data from the backend upon page load, and each action should automatically be saved (i.e. sent to the backend) immediately, no save button should be required for any action.